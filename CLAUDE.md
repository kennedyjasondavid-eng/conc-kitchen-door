# DOOR — Kitchen DOOR

> **Cross-app status may be stale here — single source of truth for cross-app facts (versions, phases, schema seams) is the HOUSE status ledger in `~/.claude/CLAUDE.md` (as of 2026-06-13).**

## HOUSE context
DOOR is one app in **HOUSE** (CONC shelter-catering ops; Hospitality Operations Unified System Engine). It sits **upstream of everything**: the resident registry + the active menu source. Pipeline: **DOOR → EXPO → HUB**; MISE/CODEX feeds recipe + allergen data *into* DOOR.
- Siblings: **EXPO** (`conc-kitchen-expo`, consumes DOOR's menu) · **HUB** (`conc-kitchen-hub`, daily board) · **MISE/CODEX** (`conc-recipe-hub`, recipe + allergen + cost source).
- Machine-global HOUSE map: `~/.claude/CLAUDE.md`. Ecosystem vision (the "why"): `…\OneDrive - CHRISTIE OSSINGTON NEIGHBOURHOOD CENTRE\~CONC Project Management Tool~\CURRENT\CONC_Kitchen_Operations_Vision_V42.html`.
- Design lessons (KNOWLEDGE owner): cross-HOUSE design wisdom + lessons live in `conc-kitchen-house/INSIGHTS.md` (the single owner) — read it before a design pass; point here, don't restate.
- Implementation seams: `conc-kitchen-house/HOUSE_PROVEN_SEAMS.md` — problem classes solved once somewhere in HOUSE (quota-safe writes, token handling, atomic multi-file publish, escaper families, …); **port the seam, don't reinvent — cite the row**. DOOR owns row 4 (`PublishAuth`) and is the named gap in rows 1 (`setItemSafe`) and 3 (`getSavedToken` trims but doesn't strip non-ASCII). **Row 5 (atomic multi-file publish) was DOOR's gap and is now closed** — `ghPushFilesAtomic` (2026-08-17, slice D1); the registry cell is updated in the paired `conc-kitchen-house` change.

## What this is
Daily operational interface for Rexdale shelter meal service. Staff enter resident changes once (intakes / discharges / restriction updates) and DOOR generates all plating sheets, dietary labels, and support files in one run. Allergen + anaphylactic routing checked **before** service.
- `index.html`, single-file HTML/CSS/JS, ~20K lines. **`DOOR_APP_VERSION = 'v31-standard.1'`** + `DOOR_BUILD_DATE = '2026-06-26'` drive a staff-visible build stamp; `menu_current.json` `_meta.version` **32**, `menu_reno.json` 2. `DOOR_SCHEMA_VERSIONS.menu_current` = 32 (mirror, gate-checked).
- **Menu source truth:** Jason's July 2 workbook import, stored as `concUploadedMenu`, is the standard-menu base. `concMenuBase` is only a post-import delta layer. A standing `standardCutover` marker prunes pre-2026-07-13 overlay days at boot, daily sync, and publish pre-merge so old reno edits cannot resurrect from another device or the cloud.
- Live: https://kennedyjasondavid-eng.github.io/conc-kitchen-door/

## Architecture
- Single-file HTML; all CSS/JS inline; no build step.
- **localStorage = live state** (residents, queue, registry). The repo's JSON files are *snapshots* the push writes for the rest of HOUSE to consume.
- Screens are **tabs toggled by `showScreen()`** (`data-screen`), not routes. All UI rendered up-front.
- Recipe + allergen data fetched **live from CODEX** (`DOOR_RECIPE_DATA.json` off the recipe-hub Pages site); a baked-in snapshot is the offline fallback. **Allergen data is owned by CODEX — fix it there, never fork it into DOOR.**
- Compliance engine: **exclude-array** pattern. **Anaphylactic flags are sacred — never silently overridden; always surfaced in red with explicit acknowledgement.**

## Published data contracts (DOOR = menu + resident source)
| File | Consumer |
|---|---|
| `menu_current.json` `{_meta, weeks[]}` (`_meta.version` is a **schema constant, not a data revision** — see the version-contract note below) | EXPO `loadMenuFromDOOR` |
| `menu_reno.json` (reno 4-week, LAN routing) | EXPO when `scheduleMode = reno` |
| `menu_overlay.json` (post-import user deltas; cutover-stamped) | applied on top of `menu_current.json` |
| `routing_by_meal.json` `{ _meta:{…, builtFromMenu:{exported,hash}}, wk: { DAY: { meal: { SectionLabel: n, _components: {dish: portions} } } } }` (`_meta.builtFromMenu` = the menu revision this routing was built from — D2, 2026-08-17; also stamped on `registry_summary`/`door_state`) | EXPO portion math (section labels) · HUB portion-aware CODEX deep-links (`_components`) |
| `registry_summary` · `meal_swaps` · `recent_log` · `learned_nr` · `custom_tag_rules` · `special_meals` · `door_state` | snapshots / informational |

**Change a published schema → update the EXPO consumer in the same change set** (or stage via menu overlays).

**⚠ Cross-repo data-contract rule (HOUSE-wide, silent-drift plan §X2).** A **fail-closed assertion about another app's data may not ship in a change set that does not also update that app.** If it must ship alone, it fails **loud and visible** (an alarm the operator sees), never into a silent fallback. The reference implementation is the DOOR→EXPO→HUB required-key contract: EXPO's `hub_meals_contract_gate.mjs` (producer guarantee) and HUB's `stage_nocard_contract.mjs` (consumer backstop) assert the identical top-level key list, so a dropped key reddens the producer's CI first and the consumer's second — neither freezes a staff board. This is the durable form of the "update the consumer in the same change set" line above.

### ⚠ The version contract — `_meta.version` is NOT what it looks like (D7, 2026-07-27)
Every consumer that reads `_meta.version` must know three facts, each proven on live data:
1. **It is a hand-maintained SCHEMA constant** (`DOOR_SCHEMA_VERSIONS.<artifact>`), **not a data revision** — content changes do not bump it. Measured 2026-07-27: **23 menu fields changed 07-15→07-27, including an allergen flag flipping `hasPork` true→false, while it stayed `32`.**
2. **It is NOT monotonic** (31 → 30 → 32 across 06-30/07-12/07-13). Only ever test **inequality**; never order-compare, never treat a lower number as older.
3. **Each artifact keeps a SEPARATE counter** (2026-07-27: registry 30 / routing 31 / menu 32). Only `menu_current.json`'s counter is comparable to a consumer's `_doorMenu.version`; comparing any other pair is meaningless.

This is why HUB's freshness check compares menu **content**, never these numbers (versions are at most a fallback inequality test). Cross-app record: `conc-kitchen-hub/HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D7.

### `_components` (added 2026-06-10, the Stage 2 portions pipe)
Each meal slot's `_components` maps every dish/side to its **total portions across all routing sections that receive it**, computed by the same engine as the plating sheets (`computePlatingData` + `getAltMeal` inside `buildRoutingByMealJSON`). HUB resolves its schedule items against this and appends `&portions=N` to CODEX recipe links, so a cook lands on the recipe pre-scaled. Notes baked into the design: `modified_main`/`bland_alt` sections eat the main meal's components; component keys merge case-insensitively; **anaphylactic residents are excluded** (they get a separate alternative) and the counts use the plating engine's smart flag defaults — so `_components` can legitimately disagree with the sibling section counts. **Never "reconcile" the two**; the plating engine is the authority. The block is try-guarded and additive — section counts stay byte-identical.

## Publish safety (hardened 2026-06-18 — PRs #48–#51)
The publish path is hardened end-to-end. `PublishAuth` centralizes credentials; the **embedded default token + shared-key fallbacks are removed** (no token resurrects on empty storage; persisting requires Test & Save; background/auto/side publishes use the saved token, and the unsaved-typed-token warning is manual-only so a value left in the field can't stall the cloud feed). All GitHub writes funnel through one serialized queue (`_ghWriteQueue`) so publishes can't race on file SHAs. Output is escaped at every display sink; localStorage + published JSON stay raw.
- **Gate-9 structural block (PR #50):** a Stop-level *structural* defect in the artifacts (missing menu day, non-integer/negative routing count, missing routing slot, malformed `_components`, missing artifact/`_meta.version`) now **blocks the publish** — auto-publish skips ("Blocked — N structural defects"), a manual publish prompts to override. Clinical/diagnostic flags stay advisory. Downstream EXPO/HUB fall back to last-good-valid via their cache rather than ingest corruption.
- **Stale-tab guard:** publishing from a tab opened before a deploy is detected (`checkForFreshDoorVersion`); auto-syncs skip, a manual publish confirms; the "publish anyway" override is scoped to manual publishes only.
- **`computeDoorComplianceDiagnostics`** is built + tested but **intentionally unwired** — the engine for a future consolidated compliance gate (the live anaphylactic net runs via `getAnaphConflictRooms`/routing lockout/plating ALERT).
- **No-build smoke harness:** `tests/door-smoke.mjs` (`node --test tests/*.mjs`) + a GitHub Actions check, 88 tests. `.gitattributes` forces `*.html`/`*.mjs` to LF (Windows edits CRLF-flipped `index.html` and broke the harness's marker extraction).

## Recent (2026-08-17) — stale-tab guard is per-tab, not per-origin (silent-drift slice D5)
Closes shape **c** (`HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D5). `checkForFreshDoorVersion` compared the live server HTML stamp against **`localStorage['door_html_stamp']`** — shared across every tab on the github.io origin. So a second tab booting fresh (or the first tab reloading) overwrote it to the newest stamp, and an **OLD-build tab then read as fresh** and could publish with outdated publish/build logic — the exact thing the guard exists to prevent.
- **Per-tab anchor.** New module var `_doorBootHtmlStamp` (per-tab by nature, per-load — a reload starts a fresh JS context and re-anchors) holds the stamp THIS tab loaded. Staleness is now decided against it, never against the shared localStorage value.
- **Pure verdict helper.** `doorTabStaleVerdict(bootStamp, liveStamp)` → `{stale, anchor}`: unanchored → anchor to live, not stale; anchored + same → fresh; anchored + newer → stale; missing live stamp → never stale. The verdict depends only on this tab's own anchor.
- **localStorage is a hint only.** `door_html_stamp` is still written (newest stamp any tab has seen) but nothing reads it as the staleness authority anymore.
- **Gates:** `door-smoke` +5 (**107 → 112**) — first boot anchors + not stale · anchored fresh vs stale · **two tabs with the same live stamp judged by their OWN anchor** (opposite verdicts — the fix) · missing live stamp never stale · a source gate that the guard reads the per-tab anchor and no longer reads the shared localStorage stamp as the baseline. Authored-to-fail: **5/5 red** against pre-D5.

## Recent (2026-08-17) — per-slot `_components` failure is no longer invisible (silent-drift slice D4)
Closes shape **b** (`HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D4). In `buildRoutingByMealJSON` each slot's `_components` computation is try-guarded; a **throw** was `console.warn`-only, and the validator raised nothing unless **all 84** slots lost their components (`componentSlots === 0`). So a partial break (a handful of slots silently losing portions) shipped unnoticed — a cook gets no `&portions=N` for those dishes.
- **Builder records the failure.** The per-slot catch now pushes `{week,day,meal,error}` into `componentErrors`; the builder stamps `_meta.componentErrors` on `routing_by_meal.json` **only when non-empty** (additive — a healthy build stamps nothing).
- **Anchored on the caught exception, not raw absence.** A slot that legitimately produces zero components (no residents route to it) is *not* recorded — that avoids the meta-cause trap. The all-empty case stays the existing `routing-components-missing` Review.
- **Validator grades it.** `routing-component-compute-error` **Review** per failed slot (the caught error reaches the publish status line); past `DOOR_COMPONENT_ERROR_STOP_THRESHOLD` (2) a single **Stop** `routing-components-degraded` blocks (Gate-9) — a broadly-broken portions pipe refuses to ship, while one transient slip stays advisory and the publish proceeds.
- **Gates:** `door-smoke` +5 (**102 → 107**) — few failures are Review-not-Stop · past-threshold Stops · a healthy artifact records nothing and raises nothing · a source gate pins the catch-records-into-accumulator + additive-stamp + placement-in-catch · a broadly-degraded routing artifact is Gate-9 blocked on auto-publish while one slip publishes. Authored-to-fail: **4/5 red** against pre-D4 (the 5th is the healthy-data guard).

## Recent (2026-08-17) — baked-menu fallback is never a silent substitution (silent-drift slice D3)
Closes shape **d** (`HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D3). `getMenuData()` falls back to the baked `MENU_DATA` constant when this device has **no uploaded menu** (`concUploadedMenu` absent — cleared, fresh browser, wiped storage). `MENU_DATA` is the OLD rotation, not the current standard base, and the publish path stamped it as the current menu — a stale menu silently reaching EXPO/HUB. **FORK-3 ruled (a): Stop with Gate-9 override semantics.**
- **`_doorMenuUsedBakedFallback`** — module flag set on every `getMenuData()` resolution (`= !uploaded`), so it always reflects the live menu source.
- **Persistent banner** `renderMenuSourceBanner()` on the Menu Config screen (the publish surface, mirrors `renderStaleFlagsBanner`): a red "Menu source: built-in fallback — re-import before publishing" warning whenever the flag is set.
- **`menu-source-fallback` Stop** in `validateDoorPublishArtifacts(artifacts, {menuSourceFallback})` — passed in explicitly (the validator stays a pure function). It flows through **Gate-9**: an **auto-publish is blocked** ("Blocked — N structural defect(s)"); a **manual publish prompts a conscious override** (the existing DOOR pattern, so a false positive costs one confirm, not an outage). `_doPublishToGitHub` reads the flag `typeof`-guarded so an undeclared test context reads false.
- **Ships alone** — no dependency on D1/D2; independent code path (menu source, not artifact provenance).
- **Gates:** `door-smoke` +5 (**97 → 102**) — the flag is true only with no uploaded menu · the validator Stops on `menuSourceFallback:true` and never manufactures it on a bare call · an auto-publish from the fallback is Gate-9 blocked while a manual publish overrides · a real-menu publish is unaffected · a source gate pins the banner element/render/wiring + the flag set-site + the publish hand-off. Authored-to-fail: **4/5 red** against pre-D3 `index.html` (the 5th is a normal-publish guard, green both ways).

## Recent (2026-08-17) — routing records which menu it was built from (silent-drift slice D2)
Closes the "two facts, never compared" shape for DOOR's own publish (`HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D2). Nothing tied `routing_by_meal.json` to a menu revision, so a menu and its routing could drift silently — a cook could be handed a portion scale-up (`_components`) computed against a different menu than the one on the board. Now every artifact in a publish SET records the menu it shipped alongside, and DOOR refuses to publish a self-inconsistent set. **Was gated on D1** (cross-artifact Stop checks were unsound while intermediate, mutually-inconsistent artifact sets were being published per-file; D1's atomic publish made a "set" a real thing).
- **`_meta.builtFromMenu = {exported, hash}`** stamped on `routing_by_meal.json` · `registry_summary.json` · `door_state.json` (the menu is the anchor, left unstamped). `exported` is the menu artifact's own timestamp (monotonic by wall-clock); `hash` is `doorContentHash` — a key-sorted djb2 over the menu **content**, so it moves on a content change even though `_meta.version` (a schema constant, D7) does not. **This is the change-detection signal FORK-4 routes consumers to instead of the version counter.**
- **Orchestrator-owned stamping.** `_doPublishToGitHub` calls `doorStampBuiltFromMenu(publishArtifacts)` right after the four artifacts are built and **before** validation, so all three consumers reference the exact same menu object even if a builder is later refactored to compute its own timestamp. The stamp is ADDITIVE + OPTIONAL — nothing downstream is required to read it yet.
- **Gate-9 cross-artifact Stop.** `validateDoorBuiltFromMenu` (in the publish-validation testable core) Stops on a stamp that names a **different** menu than the one in the set (`builtfrommenu-exported-mismatch` / `builtfrommenu-hash-mismatch`) or a malformed stamp (`builtfrommenu-malformed`); a **wholly-absent** stamp is `Review`, not Stop — a legacy/pre-D2 or partial set never blocks and the check never cries wolf on healthy data (the §1 meta-cause: a check that fires on normal operation trains everyone to ignore it). A real publish stamps all three consistently, so the healthy path is present-and-matching → **no issue at all**.
- **No consumer check added downstream (deliberate).** Per the plan, EXPO/HUB now have ONE field to check, but wiring a fail-closed consumer is a **separate** slice — landing one here would be the D0.1 mistake (shape e) that ran EXPO on a stale baked menu for three days. DOOR only made the field exist + self-consistent.
- **Gates:** `door-smoke` +9 (**88 → 97**) — content-hash is content-true + key-order stable · the orchestrator stamps consumers from the menu and leaves the menu unstamped · a coherent set has 0 Stop and 0 builtFromMenu issue · exported-desync Stops · content-desync-with-matching-timestamp Stops (the case a version number misses) · malformed stamp Stops · absent stamp is Review-not-Stop · the **real** `_doPublishToGitHub` publishes all three consumers carrying the stamp · a source gate pins stamping-before-validation + the two Stop codes. Authored-to-fail receipt: **9/9 red** against pre-D2 `index.html`, 97/97 green after. No `DOOR_SCHEMA_VERSIONS` bump — the field is additive/optional.
- **Committed artifacts carry no stamp yet** (Review, not Stop) — they self-heal to present-and-matching on DOOR's next publish; the `builtfrommenu-absent` Review is the honest state until then.

## Recent (2026-08-17) — atomic publish: one publish = one commit (silent-drift slice D1)
Closes DOOR's gap on **`HOUSE_PROVEN_SEAMS.md` row 5**, ported from EXPO's `pushFilesToGitHubAtomic` (v9.30.3) rather than reinvented. `_doPublishToGitHub` wrote its 10 artifacts as 10 separate Contents-API commits, so **9 of the 10 CI runs per publish evaluated partially-published, mutually-inconsistent artifact sets that never really existed** — and a crash mid-publish shipped one for real (the class behind the 2026-05-28 HUB incident, where a version sidecar outran its payload). Plan owner: `conc-kitchen-hub/HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §5 D1.
- **`ghPushFilesAtomic(creds, files, message)`** — Git Data API: blobs → tree atop the current tip → commit → **one** `PATCH /git/refs/heads/main` (`force:false`). The ref move is the all-or-nothing flip; everything before it is content-addressed and unreferenced, so a failure anywhere earlier leaves the repo **byte-identical**. It goes through the same `_ghWriteQueue`, so publishes still serialize against `sidePublish` single-file writes.
- **Per the seam, there is deliberately NO fallback to the per-file path** — it fails loudly so the publish is retried whole rather than half-landing. A 409/422 (the ref moved under us) rebuilds once on the **new** tip; a second conflict is pathological and surfaces.
- **The anti-clobber guards are unchanged and now shared.** `_ghEvaluateWriteGuards` is extracted and used by **both** write paths (single-file `_ghPushFileNow` and the batch), so `not_hydrated` / `empty_clobber` / `size_regression` can never drift between them. A blocked artifact drops out of the batch and is reported in exactly the `{path,skipped,reason}` shape the per-file era produced, so the "Partial publish — N/M blocked" line is unchanged. All artifacts blocked ⇒ no commit and no ref move.
- **`sidePublish` still uses the single-file `ghPushFile`** — one file is already one commit; only the 10-artifact publish needed batching.
- **Gates:** `door-smoke` +9 (**79 → 88**), authored-to-fail **9/9 red** against pre-D1 `index.html`. They pin the plan's contract — exactly one ref update per publish · every staged artifact in that one tree, built on the current tip · a failure before the PATCH moves no ref and issues no `PUT` · the queue still serializes (no interleave) · a guard-blocked artifact is excluded from the tree but still reported · all-blocked ⇒ no commit · a 422 rebuilds on the new tip and retries exactly once · `_doPublishToGitHub` makes ONE atomic call with all ten paths and no longer loops `await ghPushFile` · both write paths share the one guard evaluator.
- **Unblocks D2** (`routing_by_meal.json` recording which menu it was built from) — cross-artifact Stop checks were unsound while intermediate states were being published.

## Recent (2026-08-17) — veg-alt allergen FLOOR (issue #63 fixed) — closes two LIVE No-Egg exposures
The fix for the veg-alt lookup narrowness recorded below (2026-07-18). `getVegAltAllergenStr` is component-partial **and** name-fuzzy; `computePlatingData` used `stored || lookup || fallback`, so any non-empty lookup **replaced** the menu-flag fallback. New pure helper **`mergeVegAltAllergenFloor(lookup, fallback)`** makes the lookup a **floor**: it may only ADD to the menu's own derived allergens, never remove one. A stored operator override (`vegAllergens` in the menu editor) is a deliberate statement and still wins outright. Tokens dedupe on a canonical key so DOOR's flag vocabulary and CODEX's feed labels don't double up (`Wheat`/`GLUTEN`, `Dairy`/`MILK`, `Eggs`/`EGG`).
- **This was not only latent — two exposures were LIVE on the committed menu + feed.** Measured by booting real DOOR headless (anchor: reproduces committed routing): **W4 THU breakfast** veg alt reads *"Scrambled eggs & Vegan BF patties"* and **W4 THU dinner** reads *"Teriyaki Tofu, Vegetable **Egg Noodles**"* — both scored `_vegAltSafe: true`, which **merges No-Egg residents into the vegan pool**. The feed lookup had suppressed the fallback carrying `Eggs`. Both now score false.
- **"Complete" match ≠ accurate — why the floor is unconditional.** Issue #63's option 1 scoped the union to *partial* matches. Measured: 6 of 84 slots match every component, and their matches are still wrong dishes (`Roast Potato and Tofu Hash` → `Roast Tofu`; `Pasta` → `Vegan Baked Pasta`), and one of them (**W4 SUN breakfast**) *suppresses `EGGS`*. A fuzzy name match is not authority to clear a declared allergen, so the floor applies to every non-empty lookup.
- **Measured impact (real DOOR, all 84 slots, committed menu/registry/feed):** 31 veg-allergen strings change (all additive — **0 allergen tokens lost anywhere**), **11 `_vegAltSafe` true→false, 0 false→true** (the merge is monotone by construction, so this direction is structural, not luck). **Section counts stay byte-identical 84/84**; only `_components` moves on 8 slots, 1–2 portions each from the vegan dish to the regular dish — the No-Egg residents that were being merged into the vegan pool.
- **⚠ Committed `routing_by_meal.json` is now stale by those 8 `_components` maps** — it regenerates on DOOR's next publish **from the menu editor / app-state** (never hand-edit routing; never reconcile counts vs `_components`). Until then the upstream CODEX gate `conc-recipe-hub/tests/door_vegalt_safety_gate.mjs` reports **anchor `_components` 76/84 and exits 1** while its actual safety verdict still reads **0 `_vegAltSafe` transitions**. That red is the anchor telling the truth about a stale artifact, not broken wiring; it self-heals on the republish. (It SKIPs in MISE CI — no sibling DOOR checkout.)
- **Gates:** `door-smoke` +5 (**74 → 79**) — four unit gates on the extracted helper (partial lookup never drops a declared allergen · EGG/MUSTARD always reaches the `_vegAltSafe` regex · result is always a superset of the fallback · DOOR/CODEX label vocabularies collapse without dropping a distinct allergen) plus a source gate pinning the precedence chain so `stored || lookup || fallback` cannot come back. Authored-to-fail receipt: **5/5 red** against pre-fix `index.html`, 79/79 green after.
- **Out of scope, noted:** the Test Meal playground (`computeTestPlatingData`) keeps its own precedence — its veg flags are operator-entered, and it has no main-meal fallback to floor against. The fuzzy matcher's *wrong* matches (which over-warn) and the feed's junk allergen labels (`NOT VEGAN FRIENDLY`) are CODEX data-quality items, untouched here.

## Recent (2026-08-10) — CODEX Nigerian / Blackened-Tofu menu reconciliation (DOOR is the menu authority)
Cross-app reconciliation with EXPO (#228) + CODEX allergen authority (`DOOR_RECIPE_DATA.json`). Three W1/W3 standard-menu slot facts corrected, plus a door-smoke gate honesty fix. DOOR owns the menu; EXPO mirrors these into its baked fallback MENU.
- **#68 (`1e86833`) — Nigerian Fish + Nigerian Styled Tofu flagged Nightshades + Spicy** (W3 FRI lunch). The Nigerian pepper-sauce dishes carry tomato/pepper (Nightshades) + heat (Spicy) per CODEX; the slot flags now say so.
- **#69 (`d7b299f`) — W3 SAT dinner wording** restored toward the July-2 import. The app comma-joins main + side, so this hand-edit did not survive a publish — see the gate-#58 note below.
- **#70 (`715fe7a`) — W1 TUE lunch vegan main Roasted Tofu → Blackened Tofu + Spicy.** The regular main is Blackened fish; its intended vegan counterpart is Blackened Tofu (Jason-confirmed), so the menu's competing "Roasted Tofu" vegan main was reconciled to Blackened Tofu. CODEX classifies both blackened dishes as Soy/Fish + **Spicy** — NOT Nightshades (distinct from the Nigerian dishes). `isSpicy` flips true, which **activates DOOR's Bland alternative section** for that slot (`index.html:13475`; it's a manual meal-level flag — auto-detect can't infer "spicy" from "Blackened"). `allergens_lunch` stays "fish, soy" (spicy is never in the allergen string).
- **⚠ The publish-reverts-hand-edits footgun bit all three.** The cloud edits went to the committed JSON, but the durable authority is the DOOR app's state (`concUploadedMenu` + overlay). The first republish regenerated `menu_current.json` from app-state and **reverted #68/#69/#70** (DOOR CI went red on `main`). Resolved durably by **re-applying the Blackened Tofu swap, the Spicy flag, and the Nigerian flags in the DOOR menu editor, then republishing** — `routing_by_meal.json` regenerated to match. A committed-JSON menu edit is not durable on its own; it must be made in the editor. (Same class as the 2026-07-19 Egg-Salad `hasPork` caveat.)
- **door-smoke gate #58 pinned to the app's comma-join form** (`6b7fdf2`, test-only): the "11 repaired slots" gate asserted W3 SAT dinner exactly equals "Pepperoni Pizza **and** Seasonal Soup" (the #69 wording), but the publish deterministically emits "Pepperoni Pizza**,** Seasonal Soup". Content identical (both dishes present; the slot's flag + routing-component assertions already pass); the gate now asserts the string the app actually produces — the `eee354c` lesson (a check that fails on healthy data is not a check).
- **Gates:** the W1 TUE reconciliation test (lunch_veg = Blackened Tofu, no Roasted Tofu, `isSpicy` true, `hasNightshades` false, `hasSoy`/`hasFish`, allergens "fish, soy") + the #68 W3 FRI Nigerian Nightshades+Spicy test. **door-smoke 70/70** on the re-applied menu. Gate-fix PR: **#71**.
- **EXPO companion:** EXPO #228 (`84c07ca`) mirrors W1 TUE Blackened Tofu into the baked fallback MENU + A1a parity fixture (board 536→533). EXPO schedules off the dish + allergens, never `isSpicy`.

## Recent (2026-07-19) — pork-is-never-halal-certified guard on menu export
Surfaced by a PROOF data-check sweep (`conc-kitchen-proof#12`): three **pork** slots were flagged `halalCertifiedMeat:true` in the stored menu — **Pork Carnitas** (W1 Tue dinner), **Pork Griot** (W3 Thu dinner), **Tandoori Pork** (W4 Fri dinner). Pork is never halal-certified, and EXPO only emits a separate halal cook when `!halalCertifiedMeat` (`conc-kitchen-expo/index.html:13606` — `if (halalAlt && !flags.halalCertifiedMeat)`; the line drifted from the 13238 recorded here, behaviour unchanged), so the `true` flag made EXPO **skip the halal cook the menu names** (`_halal` = Halal Chicken Carnitas / Chicken Griot / Tandoori Chicken) — halal residents got no dish on those nights. DOOR's live detectors already guard this (`~:13713` default, `~:15595` auto-detect); the bad flags came in with **Jason's July-2 workbook import** (restored by the 2026-07-13 overlay repair), which bypassed those detectors. Jason confirmed those dishes have halal chicken alts and the DOOR menu should reflect that.
- **Durable fix:** `buildMenuJSON` now enforces the invariant on export — for any meal, `hasPork && halalCertifiedMeat ⇒ halalCertifiedMeat:false` (clones `_flags`, never mutates app state). So a stored/imported flag can't reach EXPO wrong, and a future overlay repair can't silently regress it.
- **Data:** the committed `menu_current.json` is corrected to match (3 `<meal>_flags.halalCertifiedMeat` true→false; `_slots.main.flags` is DOOR-editor-internal and left as-is). **EXPO must re-sync the menu + Generate for the halal cooks to appear** in `hub_schedule.json`.
- **Gates:** `door-smoke` +2 (now 66) — a runtime test that *runs* `buildMenuJSON` (pork forced false, legit non-pork cert stays true, app state not mutated) + a data-invariant gate that `menu_current.json` carries no `hasPork && halalCertifiedMeat` slot.
- **Egg Salad Wrap (W1 Wed lunch) stray `hasPork` — corrected in the artifact:** the vegetarian wrap was flagged `hasPork:true`, leaking a wrong `pork` allergen token onto the plating sheet and routing a **phantom Halal section** (a "Halal Egg Salad Wrap" identical to the regular). The committed `menu_current.json` now has `hasPork:false` + the regenerated allergen line (`gluten, dairy, egg`); a new `door-smoke` allergen/flag-consistency gate guards the class (now 67). **Two caveats:** (1) the phantom `Halal:56` lives in `routing_by_meal.json` (plating-engine output) and clears when DOOR **regenerates routing on the next publish** — do NOT hand-edit routing (never reconcile counts vs `_components`); (2) the invariant guard can't defend this (it's not a pork dish), so to make it durable **clear `hasPork` on the slot in DOOR's menu editor** — otherwise a republish from app-state reverts it.

## Recent (2026-07-18) — veg-alt allergen-lookup follow-up (issue #63) — ✅ FIXED 2026-08-17 (see the entry above)
Upstream CODEX plant-forward work (`conc-recipe-hub` PRs #71/#72 — plant sides re-tagged `regular → vegan`, Jason-adjudicated) **exposed a pre-existing narrowness in `getVegAltAllergenStr`** (~`index.html:16382`): it fuzzy-matches the veg-alt text against *vegan-stream* feed recipes and can latch onto a **side** while **missing the veg main**, so a newly-vegan side's narrow allergens replace the broad main-meal fallback in `computePlatingData` (~`:19735`). Result: **3 latent veg-alt advisory-warning deltas** — Wk2 MON dinner drops PEANUTS, Wk4 SAT dinner drops PEANUTS+TREE NUTS (the veg mains — Vegetarian Peanut Stew / Massaman — genuinely contain them), Wk4 SAT lunch drops a false-positive DAIRY (that plate is all-vegan).
- **Latent** — manifests only when DOOR **next republishes** `routing_by_meal.json`; nothing on a plating sheet changed today. **The No-Egg `_vegAltSafe` net had 0 transitions on both flips, and the anaphylactic routing lockout (`getAnaphConflictRooms`, keyed off the untouched menu `_flags`) is a separate, unaffected net.** The CODEX data is correct; the fix is DOOR-side.
- **Tracked in `conc-kitchen-door#63`** with the 3 slots + fix options (union the fallback as an allergen floor when the lookup is partial / also match the veg main / add stored overrides). Verification harness lives upstream: `conc-recipe-hub/tests/door_vegalt_safety_gate.mjs` boots DOOR's real plating headless and asserts 0 `_vegAltSafe` transitions (anchor reproduces the committed routing 84/84) — re-run it against a fixed DOOR before the next routing republish.

## Recent (2026-06-26) — STANDARD menu cutover
- **`menu_current.json` → v31 (the new STANDARD menu), LIVE on `main` `5fdce16`.** The reno → standard cutover (DOOR → EXPO → HUB): DOOR is the menu source. Slot `_flags` carry union-of-streams allergens (anaphylaxis-safe — never under-flag) with two exceptions kept regular-only: `halalCertifiedMeat` and meat flags masked on non-main components. Allergens are CODEX-verified + Jason-confirmed for the new dishes (`menu_v31_allergen_*` artifacts).
- **Halal fix:** `halalCertifiedMeat=false` on every `hasPork` slot. EXPO's decomposer emits a separate halal cook only when `halalCertifiedMeat===false`; 5 pork slots (Tandoori, Souvlaki, Adobo, Al Pastor, Sausages & Mash) were wrongly `true`, so halal residents got **no** cook. (One genuine remaining gap: **W1 MON "Fully Loaded Sausage"** has no halal option defined.)
- **Build stamp** → `v31-standard.1` / `2026-06-26`; `DOOR_SCHEMA_VERSIONS.menu_current` → 31 (door-smoke gate-checked, 55/55).
- `routing_by_meal.json` was regenerated for the standard cutover. The later July 2 workbook import is now the app-state authority; see the 2026-07-13 repair below.

## ⚠ Open remediation line — silent drift (2026-07-27)
Cross-app plan + status owner: **`conc-kitchen-hub/HOUSE_SILENT_DRIFT_ACTION_PLAN_2026-07-27.md` §10**; pickup: `HOUSE_SILENT_DRIFT_HANDOFF_2026-07-27.md`. DOOR-side slices D1–D7 are listed there. Two things to know before touching DOOR's publish path or its harness:
- **`eee354c` fixed a permanently-red CI.** `tests/door-smoke.mjs` asserted that `menu_overlay.json` carried **no** week keys, which froze the 2026-07-13 post-repair state as a forever-invariant. Two real allergen fixes made through the UI on 07-19 (stray `hasPork` on W1 WED Egg Salad Wrap; the W2 SAT halal main) put week entries back — legitimately, since the overlay IS the published user-delta layer — and CI went red on all 10 commits of every publish for nine days on healthy data. It now asserts the invariant that matters: **no overlay entry may contradict the canonical menu** (that disagreement is the contamination signature; agreement is harmless redundancy). Verified: the pre-repair overlay fails on 43 conflicting fields, today's passes. **Not run under `node --test` — CI is the first real execution.**
- **`_meta.version` is a SCHEMA constant (`DOOR_SCHEMA_VERSIONS.<artifact>`), not a data revision, and it is NOT monotonic** (31→30→32 across 06-30/07-12/07-13). 23 menu fields changed 07-15→07-27 — including an allergen flag flipping `hasPork` true→false — while it stayed `32`. Separate counters exist per artifact (registry 30 / routing 31 / menu 32 today). **Never order-compare it; only `menu_current.json`'s counter is comparable to a consumer's `_doorMenu.version`.** Slice D7 documents this properly.
- ~~**One publish is 10 commits** (per-file `_ghPushFileNow`), so 9 CI runs per publish evaluate partially-published artifact sets that never existed. Slice D1 ports EXPO's `pushFilesToGitHubAtomic`.~~ **DONE 2026-08-17 — one publish is now one commit** (`ghPushFilesAtomic`); see the Recent entry. ~~**D2 is unblocked** (it was gated on D1).~~ **D2 DONE 2026-08-17** — routing/registry/state stamp `_meta.builtFromMenu:{exported,hash}`; Gate-9 Stops a self-inconsistent set (see the Recent entry). **D3 DONE 2026-08-17** — `getMenuData()` baked-fallback flagged + banner + `menu-source-fallback` Gate-9 Stop. **D4 DONE 2026-08-17** — per-slot `_components` compute failures recorded in `_meta.componentErrors`; validator Reviews each + Stops past threshold 2. **D5 DONE 2026-08-17** — stale-tab guard anchored per-tab (`_doorBootHtmlStamp` / `doorTabStaleVerdict`) instead of shared localStorage (see the Recent entries). **All DOOR silent-drift code slices are now complete (D1–D5, D7).** Remaining: D6 (v32 text regressions — **data fix, not code**: `Strogonoff`, doubled biscuit/`Brocolli`, duplicated peas; needs a workbook re-import or a DOOR-editor edit with a `_meta.manualEdits` breadcrumb).

## Recent (2026-07-13) — overlay contamination repair
- `menu_current.json` → v32. Eleven stale overlay slots were restored from Jason's July 2 workbook import, including their complete meal fields and allergen flags; matching `_components` routing slots were regenerated.
- `menu_overlay.json` was reduced to metadata plus `standardCutover`. Every overlay merge normalizes both sides and rejects week/day entries without the current marker; `_meta` is never treated as a week.
- The Menu Config **Alt Menu** toggle and its `menu_reno.json` cache/fetch paths are retired. Permanent/Edit Menu writes have a legacy-source guard, and boot clears retired alt-source state.

## Recent (2026-06-18)
- Security + stability hardening (auth, output-encoding, stability gates) — PR #48 (`b716a24`); originally a Codex cloud session, reviewed against the HOUSE VISION/INSIGHTS before landing.
- Review fixes (publish red-on-Stop, no reno-overlay contamination) + L11104 overlay write-ordering + accessibility safe wins + AA contrast — PRs #48/#49.
- Gate-9 publish-blocking + scoped stale override — PR #50 (`cf6ec84`).
- **Recipe slot autosave now applies stream filters** (`recipeMatchesSlotDef`) so exact typed *off-stream* names no longer recipe-link and autofill the wrong allergen flags. Authored in a parallel CODEX/MISE session as the Gate-U3 prerequisite — PR #51 (`3ba4322`).

## ⚠️ Reno-menu footgun (shared with EXPO)
`menu_reno.json` is **NOT** in DOOR's normal publish set — it remains a static artifact generated from the Excel sources (`_gen_menu_reno.py`, in the EXPO repo), but DOOR no longer offers an Alt Menu view or edit path for it. Reno-menu fixes belong in the Excel + generator flow; a hand edit must leave a `_meta.manualEdits` breadcrumb so the next regen does not silently undo it.

## Decisions — out of scope (2026-06-18)
The "Elegance" advisory stream's structural proposals are **retired**, not deferred: in-file modularization, the namespace/IIFE convention, section banners, and standalone design tokens. They serve maker-comfort, not the telos (a tool that recedes / fewer staff errors), and modularizing the single-file app fights the string-matching test harness for no staff benefit. The accessibility *safe wins* (focus ring, keyboard nav, SR-announced banners, AA contrast) already landed; touch-targets + print fidelity remain a live-preview task with the architect.

## Rules
- Single-file HTML; no build tools/npm/frameworks. Graceful degradation from `file://`.
- localStorage is the user's data; architect changes via seeds, user edits in-app.
- Push: `KitchenDOOR_Push.hta` (Save → Sync → push; auto-detects home/work OneDrive path). **OneDrive-only working folder — no second clone.**
- Future **HOUSE Phase 5:** SharePoint integration via Microsoft Graph API (post-renovation) — persistence layers are structured to swap to SharePoint calls one function at a time.
- Full reference: `README.md`.
